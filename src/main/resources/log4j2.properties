; name=PropertiesConfig

; property.filename = logs

; appender.console.type = Console
; appender.console.name = STDOUT
; appender.console.layout.type = PatternLayout
; appender.console.layout.pattern = [%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

; appender.uploadFile.type = File
; appender.uploadFile.name = LOGFILE
; appender.uploadFile.fileName=${filename}/propertieslogs.log
; appender.uploadFile.layout.type=PatternLayout
; appender.uploadFile.layout.pattern=[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %c{1} - %msg%n

; loggers=f
; logger.f.name=com.tcs
; logger.f.level = debug
; logger.f.appenderRefs = fi
; logger.f.appenderRef.fi.ref = LOGFILE

;logger.uploadFile.additivity = false
; rootLogger.level = debug
; rootLogger.appenderRefs = stdout
; rootLogger.appenderRef.stdout.ref = STDOUT