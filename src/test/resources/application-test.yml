spring:
  datasource:
    url: jdbc:h2:mem:ocd-db
    username: sa
    password: ''
    driver-class-name: org.h2.Driver
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create
    show-sql: true